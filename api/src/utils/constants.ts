export enum ROUTES {
    ACCOUNT = 'accounts',
}
  
export enum SERVICES {
    ACCOUNT = 'ACCOUNT_SERVICE',
}

export enum BASIC_SERVICE_ACTIONS {
    FIND = `find`,
    CREATE = `create`,
    DELETE = `delete`,
    UPDATE = `update`
}